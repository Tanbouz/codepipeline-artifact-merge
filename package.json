{
  "name": "codepipeline-artifact-merge",
  "version": "1.0.0",
  "description": "Merge artifacts in AWS CodePipeline into a single artifact using AWS Lambda",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src/index.js -o dist/index.js && cp package.json dist/package.json && cd dist && npm install --production"
  },
  "author": "s.tanbouz",
  "repository": {
    "type": "git",
    "url": "git://github.com/username/repository.git"
  },
  "license": "MIT",
  "dependencies": {
    "jszip": "^3.1.5"
  }
}
